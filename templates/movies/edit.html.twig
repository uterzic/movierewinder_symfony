{% extends "./base.html.twig" %}

{% block body %}
<div class="w-4/5 m-auto text-center">
    <h1 class="text-6xl text-center p-8">
        Edit review
    </h1>

    {{ form_start(form) }}

        {{ form_widget(form) }}
        
        <button    
            type="submit"
            class="mt-8 uppercase bg-cyan-800 text-gray-100 text-lg font-extrabold py-4 px-8 rounded-3xl">
            Update review
        </button>

    {{ form_end(form) }}
</div>
{% endblock %}